import { TColors } from "./colors";

export type TMethodParams = {
  content: any;
  identifier?: string;
  identifierColor?: TColors;
  identifierBg?: TColors;
  badgeColor?: TColors;
  badgeBg?: TColors;
};
